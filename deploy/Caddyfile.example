# Add this block to your VPS Caddyfile (e.g. /etc/caddy/Caddyfile), then
# `caddy reload`. Caddy auto-provisions TLS and transparently upgrades the
# /rt WebSocket connection.
api.njakasoa.xyz {
	encode zstd gzip
	reverse_proxy 127.0.0.1:3000
}
